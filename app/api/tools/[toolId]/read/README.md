# Tool Read API (`/api/tools/[toolId]/read`)

**Method:** `GET`

## Purpose
Retrieves the full definition (source graph) of a specific tool.

## Usage
Used by the Tool Editor canvas to load the tool for editing.

