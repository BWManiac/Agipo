/**
 * Convenience hook for accessing the workflow store.
 * Provides direct access to the Zustand store without needing to import useWorkflowStore directly.
 * Follows the pattern from tools/editor for consistency.
 */
import { useWorkflowStore } from "../store";

export { useWorkflowStore };


